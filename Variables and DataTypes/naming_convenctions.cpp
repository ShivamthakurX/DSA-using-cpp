//The name should start with an underscore(_) or a letter.
//The name can only contain uppercase & lowercase letters, digits from 0 to 9 and underscore.
//It must not be a keyboard (reserved words). Keywords have a special meaning for the compiler.